<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.katelee.widget.app">

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:icon="@drawable/ic_launcher"
                 android:theme="@style/AppTheme">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
                android:name=".AutoHidingHeaderActivity"
                android:label="@string/AutoHidingHeader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="category_demo_code" />
            </intent-filter>
        </activity>

        <activity
                android:name=".ParallaxScrollingHeaderActivity"
                android:label="@string/ParallaxScrollingHeader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="category_demo_code" />
            </intent-filter>
        </activity>

    </application>

</manifest>
